const contacts=(state,{type,payload})=>{
    switch (type) {
        case "CONTACT_DETAILS":
            return state;
           
    
        default:
            return state;
    }
};

export default contacts;